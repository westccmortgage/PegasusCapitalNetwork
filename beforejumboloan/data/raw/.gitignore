# Official source files are large and are NOT committed. Drop them here and run
# the import scripts. The small *.sample.csv files (format references / seed)
# ARE committed so the pipeline runs out of the box.
*.xlsx
*.xls
*.csv
!*.sample.csv
# The CY2026 official FHFA flat file IS committed so `npm run import:all`
# reproduces the official dataset (the HUD CHUMS .txt sources are committed too).
!fhfa-conforming-all-counties-2026.csv
