/* PEGASUS v69 — Config. Publishable (anon) key is safe for the browser. */
window.PEG_CONFIG = {
  SUPABASE_URL: 'https://trdwsssouhpawhfdkfqf.supabase.co',
  SUPABASE_ANON: 'sb_publishable_hrR8wI6hM921mnxk7bBE_A_YKxWsoBI',
  // Preview mode: when true (or when no Supabase session exists), the store
  // hydrates from seeded demo data so the site renders without a backend.
  // Set to false in production once auth is wired and you want hard gating.
  ALLOW_DEMO_FALLBACK: false,
};
